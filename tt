class Solution:
    def kidsWithCandies(self, candies: List[int], extraCandies: int) -> List[bool]:
        for i in List:
            candies = List[i] + extraCandies
        return candies

        while candies:
            for i in candies:
                if (List[i] > List[i-1]):
                    return true 
